The "track" subcommand
======================

.. note:: The ``track`` subcommand has been retired. All backend Linux
   repositories now share objects with each other, so this command no longer
   serves any useful purpose.
